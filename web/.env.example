# MPGS server URL for a packaged (non-proxied) build. Leave empty for browser/
# Tauri dev, which proxies through Vite. The local mpgs-server default is
# http://127.0.0.1:17880. Packaged desktop is a pure client: enter the origin
# on first launch (no baked-in base URL).
VITE_MPGS_API_BASE=
